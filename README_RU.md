# FFConverter — Универсальный конвертер данных v2.0 (GUI)

[![License](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)
[![Python](https://img.shields.io/badge/python-3.8%2B-3776AB.svg)](https://www.python.org/)
[![GUI](https://img.shields.io/badge/UI-Tkinter-1f6feb.svg)](#)
[![Version](https://img.shields.io/badge/version-2.0-brightgreen.svg)](#)
[![Pandas](https://img.shields.io/badge/pandas-required-150458.svg)](https://pandas.pydata.org/)
[![OpenPyXL](https://img.shields.io/badge/openpyxl-required-019733.svg)](https://openpyxl.readthedocs.io/)
[![Tabulate](https://img.shields.io/badge/tabulate-required-4C9A2A.svg)](#)
[![DragDrop](https://img.shields.io/badge/drag%26drop-optional-orange.svg)](#)

FFConverter v2.0 — это продвинутое десктопное GUI-приложение, построенное на Tkinter, для конвертации файлов структурированных данных между различными форматами: CSV, XLSX, JSON, XML, YAML, INI, TXT и Markdown. Программа включает поддержку drag & drop, комплексное управление настройками, валидацию данных, систему логирования и поддерживает открытие файлов исходного кода на многих языках программирования с возможностью предпросмотра.

## Ключевые особенности

### Основная функциональность
- **Автоопределение** входного формата по расширению файла и интеллектуальное зондирование содержимого (эвристики JSON/XML/YAML/INI)
- **Поддерживаемые входные форматы**: csv, xlsx, json, xml, yaml/yml, ini, txt, md; файлы исходного кода определяются отдельно
- **Поддерживаемые выходные форматы**: csv, xlsx, json, xml, yaml, ini, txt, md
- **Поддержка исходного кода**: Python, C/C++, Java, C#, JavaScript, TypeScript, Go, Ruby, Swift, Shell, Batch, Perl, PHP, Rust, Scala, Kotlin, Dart
- **Файлы кода** могут быть сохранены только как TXT или MD (сохраняет строки; без парсинга)

### Продвинутые возможности GUI
- **Тёмная тема интерфейса** с современным стилем и кастомной цветовой схемой
- **Поддержка Drag & Drop** (опциональная зависимость tkinterdnd2)
- **Панель живого предпросмотра** с настраиваемым количеством строк/записей и рендерингом DataFrame в Markdown
- **Индикаторы прогресса** с многопоточными операциями для лучшей отзывчивости
- **Окно настроек** с конфигурируемыми параметрами GUI и обработки
- **Обновления статуса** и комплексная обработка ошибок

### Системные функции
- **Управление конфигурацией** с постоянными настройками (settings.ini)
- **Комплексное логирование** с ежедневными лог-файлами и отслеживанием операций
- **Валидация данных** с ограничениями размера файлов и проверками доступа
- **Многопоточность** для неблокирующих файловых операций
- **Восстановление после ошибок** с детальными отчётами об ошибках и трассировкой

## Требования

- **Python 3.8+** на Windows/macOS/Linux
- **Обязательные пакеты**: pandas, pyyaml, openpyxl, tabulate, lxml
- **Опциональные пакеты**: tkinterdnd2 (для функциональности drag & drop)
- Внешние бинарные файлы не нужны; все конвертации выполняются в Python с использованием pandas, PyYAML, OpenPyXL, ConfigParser и ElementTree

## Установка

# 1) Клонируйте репозиторий
git clone https://github.com/VioletSoul/FFConverter.git
cd FFConverter

# 2) (Опционально) Создайте и активируйте виртуальное окружение
python -m venv .venv
# Windows
.venv\Scripts\activate
# macOS/Linux
source .venv/bin/activate

# 3) Установите обязательные зависимости
pip install pandas pyyaml openpyxl tabulate lxml

# 4) (Опционально) Установите поддержку drag & drop
pip install tkinterdnd2

Если зависимости отсутствуют, приложение покажет детальные инструкции по установке и корректно завершит работу.

## Запуск

python ffconverter.py

Это запустит окно Tkinter с заголовком "Универсальный конвертер данных v2.0". Вы можете:
- Использовать кнопку "📁 Выбрать файл" для открытия поддерживаемого файла
- **Перетаскивать файлы** прямо в окно (если установлен tkinterdnd2)
- Выбрать целевой формат в выпадающем списке и нажать "🔄 Конвертировать"
- Получить доступ к настройкам через кнопку "⚙ Настройки"

## Конфигурация и настройки

Приложение включает комплексную систему настроек, доступную через окно настроек:

### Настройки GUI
- **Размер окна**: Настраиваемая ширина и высота
- **Размер шрифта**: Регулируется для лучшей читаемости (8-20px)
- **Строки предпросмотра**: Настройка количества строк в предпросмотре (5-100)

### Настройки обработки
- **Ограничения размера файлов**: Максимальный размер файла в МБ (по умолчанию: 100МБ)
- **Валидация данных**: Включение/выключение проверок валидации файлов
- **Индикаторы прогресса**: Показ/скрытие прогресс-баров во время операций

Настройки автоматически сохраняются в `settings.ini` и сохраняются между сессиями.

## Система логирования

FFConverter v2.0 включает комплексное логирование:
- **Ежедневные лог-файлы** хранятся в директории `logs/`
- **Отслеживание операций** для чтения, записи файлов и ошибок
- **Автоматическая ротация логов** с именами файлов по датам
- **Детальные отчёты об ошибках** с полными трассировками

Лог-файлы именуются как: `converter_YYYYMMDD.log`

## Определение формата

- **Определение по расширению** для быстрой маршрутизации: .csv, .xlsx, .json, .xml, .yaml/.yml, .ini, .txt, .md
- **Определение исходного кода** для языков программирования: .py, .cpp, .c, .h, .java, .cs, .js, .ts, .go, .rb, .swift, .sh, .bat, .pl, .php, .rs, .scala, .kt, .dart
- **Резервное определение по содержимому**: Зондирует первые ~2048 символов для идентификации JSON, XML, YAML или INI через безопасные парсеры
- **Умолчания по умолчанию** к TXT когда формат неоднозначен

## Логика обработки данных

### Логика чтения
- **Файлы кода**: Читаются как список строк (без парсинга, сохраняет форматирование)
- **CSV/XLSX**: Загружаются в pandas DataFrame с автоопределением кодировки
- **JSON/YAML**: Списки → DataFrame; словари → строка DataFrame если возможно, иначе сохраняются как словарь
- **XML**: Попытка извлечения таблицы путём итерации дочерних элементов в записи; отказ к словароподобному корневому отображению
- **INI**: ConfigParser в словарь секций; транспонирован в DataFrame для табличного вида
- **TXT/MD**: Читаются как сырые строки для предпросмотра и конвертации

### Логика сохранения
- **CSV**: `DataFrame.to_csv(index=False)` с кодировкой UTF-8
- **XLSX**: `DataFrame.to_excel(index=False)` через openpyxl
- **JSON**: `DataFrame.to_json(orient="records", indent=2)` с поддержкой Unicode
- **XML**: Строит `<records><record>…</record></records>` с XML-безопасными тегами и экранированным текстом
- **YAML**: Выгружает записи DataFrame как список словарей с поддержкой Unicode
- **INI**: Каждая строка DataFrame становится секцией с отображением столбец→значение
- **Markdown**: `DataFrame.to_markdown(index=False)` требует tabulate
- **TXT**: `DataFrame.to_string(index=False)` для выровненных текстовых таблиц
- **Сохранение кода**: Исходный код сохраняется дословно только в TXT/MD

## Улучшения GUI

### Визуальный дизайн
- **Тёмная тема** с кастомной цветовой схемой для лучшего пользовательского опыта
- **Современный стиль** с кастомными шрифтами и улучшенными отступами
- **Адаптивная компоновка** которая подстраивается под разные размеры окон
- **Профессиональный внешний вид** с акцентными цветами и последовательным дизайном

### Пользовательский опыт
- **Живые обновления предпросмотра** при изменении количества строк предпросмотра
- **Многопоточные операции** предотвращают зависание GUI во время обработки файлов
- **Индикаторы прогресса** показывают статус операций
- **Комплексные сообщения об ошибках** с дружелюбными к пользователю объяснениями
- **Визуальная обратная связь drag & drop** когда поддерживается

## Архитектура

### Основные классы

class AppLogger:          # Система логирования с ежедневной ротацией
class AppConfig:          # Управление конфигурацией (settings.ini)
class DataValidator:      # Валидация файлов и проверки безопасности
class SettingsWindow:     # Окно управления настройками GUI
class DataConverterGUI:   # Главное окно приложения и логика

### Ключевые константы

SUPPORTED_FORMATS = ["csv", "xlsx", "json", "xml", "yaml", "ini", "txt", "md"]
SOURCE_EXTS = [".py", ".cpp", ".c", ".h", ".java", ".cs", ".js", ".ts", ".go",
".rb", ".swift", ".sh", ".bat", ".pl", ".php", ".rs", ".scala",
".kt", ".dart"]

## Примечания по использованию и ограничения

- **Сложные вложенные данные**: Конвертация глубоко вложенных JSON/YAML/XML может потребовать ручного выравнивания
- **Ограничения XML-схемы**: Простой вывод на основе записей; атрибуты и сложные иерархии не сохраняются
- **Обработка исходного кода**: Файлы рассматриваются как простой текст без подсветки синтаксиса или парсинга
- **Соображения памяти**: Большие файлы обрабатываются полностью в памяти; ограничения размера настраиваются
- **Поддержка Unicode**: Полная поддержка UTF-8 для международных символов во всех форматах

## Продвинутые функции

### Валидация данных
- **Проверки доступности файлов** перед обработкой
- **Валидация размера** с настраиваемыми ограничениями (по умолчанию 100МБ)
- **Верификация формата** с интеллектуальным восстановлением после ошибок
- **Валидация путей** для выходных директорий

### Оптимизации производительности
- **Многопоточные файловые операции** для отзывчивого UI
- **Эффективное использование памяти** со стримингом где возможно
- **Отслеживание прогресса** для долго выполняющихся операций
- **Настраиваемые ограничения обработки** для предотвращения перегрузки системы

### Обработка ошибок
- **Плавная деградация** когда опциональные функции недоступны
- **Детальные сообщения об ошибках** с действенными решениями
- **Комплексное логирование** для отладки и устранения неполадок
- **Дружелюбные к пользователю уведомления** через диалоговые окна

## Устранение неполадок

### Проблемы с зависимостями

# Установить все необходимые пакеты
pip install pandas pyyaml openpyxl tabulate lxml

# Для поддержки drag & drop
pip install tkinterdnd2

### Частые проблемы
- **Проблемы сохранения Excel**: Убедитесь, что openpyxl установлен и путь вывода доступен для записи
- **Ошибки YAML/JSON**: Проверьте кодировку UTF-8 и валидный синтаксис
- **Ошибки парсинга XML**: Убедитесь в правильно сформированной XML структуре
- **Ошибки памяти**: Уменьшите ограничения размера файлов в настройках или разделите большие файлы
- **Ошибки разрешений**: Проверьте разрешения на файлы и директории
- **Drag & drop не работает**: Установите пакет tkinterdnd2

### Анализ логов
Проверьте директорию `logs/` для детальных логов операций и информации об ошибках:

# Просмотреть последние логи
tail -f logs/converter_20250915.log

## Разработка

### Архитектурные особенности
- **Одно-файловая архитектура** использующая стандартную библиотеку Tkinter с модульным дизайном классов
- **Расширяемая поддержка форматов** через словари обработчиков
- **Поведение, управляемое конфигурацией** для лёгкой настройки
- **Комплексная обработка ошибок** с интеграцией логирования
- **Чистое разделение** GUI, логики и вспомогательных функций

### Добавление новых форматов

Для добавления поддержки новых форматов обновите эти словари:

READERS = {"format": reader_function}
WRITERS = {"format": writer_function}
SUPPORTED_FORMATS.append("new_format")

### Кастомизация
- Измените цветовые константы для разных тем
- Настройте стандартные параметры в `AppConfig.create_default_config()`
- Расширьте правила валидации в классе `DataValidator`

## История версий

### v2.0 Новые функции
- **Система управления настройками** с постоянной конфигурацией
- **Комплексное логирование** с ежедневной ротацией и отслеживанием операций
- **Валидация данных** с ограничениями размера файлов и проверками безопасности
- **Поддержка drag & drop** (опциональная интеграция tkinterdnd2)
- **Многопоточность** для неблокирующих файловых операций
- **Индикаторы прогресса** с визуальной обратной связью
- **Улучшенная обработка ошибок** с детальными отчётами
- **Современная тёмная тема** с улучшенным UI/UX
- **Модульная архитектура** с разделёнными обязанностями

## Лицензия

MIT License. Смотрите LICENSE для деталей.

## Поддержка

Если вы столкнулись с проблемами:
1. Проверьте директорию `logs/` для деталей ошибок
2. Убедитесь, что все зависимости установлены
3. Просмотрите раздел устранения неполадок
4. Откройте issue на GitHub с лог-файлами и информацией о системе
